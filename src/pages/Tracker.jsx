export default function Tracker() {
  return (
    <div className="page-layout">
      <div className="page-header">
        <h2>Health Tracker</h2>
        <p style={{ color: '#64748B', fontSize: '14px' }}>Log your daily vitals here.</p>
      </div>
      {/* We can build this out later if we have time! */}
    </div>
  );
}